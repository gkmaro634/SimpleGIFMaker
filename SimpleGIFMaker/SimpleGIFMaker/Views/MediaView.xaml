<UserControl x:Class="SimpleGIFMaker.Views.MediaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:SimpleGIFMaker.Views"
             xmlns:ctrls="clr-namespace:SimpleGIFMaker.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="600"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="mediaRegion" Grid.Row="0" Width="600" Height="600" Background="LightGray">
            <Viewbox Stretch="Uniform" MaxWidth="{Binding ElementName=mediaRegion, Path=ActualWidth}" MaxHeight="{Binding ElementName=mediaRegion, Path=ActualHeight}">
                <Viewbox.LayoutTransform>
                    <RotateTransform 
                            Angle="{Binding Rotation, Mode=OneWay}"
                            CenterX="{Binding CenterX, Mode=OneWay}" 
                            CenterY="{Binding CenterY, Mode=OneWay}"
                            />
                </Viewbox.LayoutTransform>
                <Grid Background="LightGray">
                    <!--<Border x:Name="DummyMediaImage" BorderThickness="8" BorderBrush="Gray" Background="Gray"
                        Width="1500" Height="{Binding MediaHeight}" Margin="0"/>-->

                    <MediaElement
                    x:Name="media"
                    Source="{Binding MovieFilePath, Mode=OneWay}"
                    LoadedBehavior="Play"
                    Volume="0"
                    Panel.ZIndex="1"
                    >
                        <!--<MediaElement.LayoutTransform>
                        <RotateTransform 
                            Angle="{Binding Rotation, Mode=OneWay}"
                            CenterX="{Binding CenterX, Mode=OneWay}" 
                            CenterY="{Binding CenterY, Mode=OneWay}"
                            />
                    </MediaElement.RenderTransform>-->
                    </MediaElement>

                    <ctrls:DraggableRectangle
                    Width="{Binding MediaWidth, Mode=OneWay}" 
                    Height="{Binding MediaHeight, Mode=OneWay}"
                    StartX="100"
                    StartY="200"
                    EndX="1000"
                    EndY="800"
                    Panel.ZIndex="2"
                    />
                </Grid>
            </Viewbox>
        </Grid>


        <Grid Grid.Row="1" Width="{Binding ElementName=mediaRegion, Path=ActualWidth}" Height="32"
              >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Content=">" Grid.Column="0"/>
            <Slider x:Name="seekBar" Grid.Column="1"/>
            <TextBlock Grid.Column="2" Text="00:00:23" Width="48" HorizontalAlignment="Right"/>
        </Grid>

        <TabControl Grid.Row="2">
            <TabItem Header="GIF Convert">
                <views:ConvertControlView/>
            </TabItem>
            <TabItem Header="Crop">
                <views:CropSettingView/>
            </TabItem>
            <TabItem Header="Cut">
                <views:CutSettingView/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
